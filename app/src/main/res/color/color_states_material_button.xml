<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disable background -->
    <item android:state_enabled="false"
        android:color="@color/disabled_btn_color"/>

    <!-- Enabled background -->
    <item android:color="@color/default_btn_color"/>

</selector>